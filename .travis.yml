language: julia
codecov: true
coveralls: true

sudo: required

os:
  - linux
  - osx

julia:
  - 1.1
  - 1.2
  - 1.3
  - 1.4
  - nightly

matrix:
  allow_failures:
  - julia: nightly

notifications:
  email: false

addons:
  apt_packages:
    - gfortran
